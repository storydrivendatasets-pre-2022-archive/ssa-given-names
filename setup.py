from setuptools import find_packages, setup

setup(
    name='src',
    packages=find_packages(),
    version='0.0.1',
    description='Name counts (mostly babies) based on Social Security Administration card applications.',
    author='Dan Nguyen',
    license='MIT',
)
